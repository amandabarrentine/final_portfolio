<div class="row">
            <div class="box">
                <div class="col-lg-12">
            <h1>Mercer News</h1>
            <ul>
                <% @results.each do |result| %>
                    <li>
                        <a href ="<%= result.uri %>"><%= result.title %></a> <%= result.content %>
                    </li>
                <% end %>
            </ul>
                
        </div>
    </div>
</div>